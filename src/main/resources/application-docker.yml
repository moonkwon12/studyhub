spring:
  datasource:
    url: jdbc:mysql://db:3306/studyhub?serverTimezone=Asia/Seoul
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        hbm2ddl.auto: create
    open-in-view: false
